# D1 读取额度优化版升级

本次修改针对 `/api/codes`：收信时计算并保存 `recipient_hash`，查询时以 MD5 等值和时间范围使用索引，取消扫描近期全部收件地址及逐条计算哈希。接口仍最多返回 5 条，MD5 链接和响应字段不变。

普通查询明确使用 `idx_email_code_hash_time`，全量查询使用 `idx_email_code_time_v2`。域名与发件人白名单继续生效；不是每个请求固定只读取 5 行，筛选条件会影响实际扫描量。每次成功查询读取超过 100 行会记录 `Code query rows_read`，不记录邮箱、验证码或链接参数。

## 已有实例：先升级数据库，再部署

1. 如当天 D1 额度已耗尽，先等北京时间上午 8 点重置，或自行升级 Workers 付费套餐。代码改动不能恢复已耗尽额度。
2. 临时关闭验证码页面/API调用方的轮询，避免迁移期间持续请求。
3. 在 `cloud-mail/mail-worker` 目录执行（把 `<数据库名称>` 换成自己的 D1 名称）：

   ```sh
   pnpm exec wrangler d1 execute <数据库名称> --remote --file=sql/code-hash-upgrade.sql
   ```

   此文件增加 `email.recipient_hash` 字段及索引，不删除邮件。**ALTER TABLE 只运行一次**。若已执行过，或部分成功后出现“字段已存在”，先在 D1 控制台查看 `PRAGMA table_info(email);`，确认字段存在后只执行文件中的两条 `CREATE INDEX IF NOT EXISTS`，无需重建数据库。

4. 部署最新 Worker（GitHub 自动构建的实例需在构建发布前完成第 3 步）。初始化新实例时新代码会自动创建字段及索引。
5. 恢复调用，新收到邮件会在写入时保存哈希。历史邮件保留，但不批量扫描回填哈希，避免消耗读取和写入额度；升级前邮件可能在其剩余五分钟窗口内无法通过普通 MD5 查询。无需重发历史邮件或执行全表 UPDATE。

缺少新字段/索引时接口会返回 503，故意不回退到旧的高读取量扫描逻辑。部署新收信代码前也必须完成迁移，否则邮件入库会失败。回滚代码不需要删除新增字段与索引。

## 降低无效轮询

- 正常轮询至少间隔 3 秒；当前页面定时器每 2 秒检查一次是否到期，实际正常请求约每 4 秒一次。
- 连续空结果逐步延长至 5 秒、15 秒；后台标签页不请求；缺少 recipient 不查询数据库。
- 错误从 5 秒开始指数退避，上限 60 秒；遵守服务端 503 的 `Retry-After`。
- 400/404/405 停止当前页面自动请求，修改参数/配置后重新打开页面。
- D1 配额错误触发当前 Worker 实例 60 秒冷却，期间不重复查询 D1。实例间不共享此状态，因此不是全局限流。
- 不缓存验证码结果，避免缓存导致已删除验证码继续显示；前端继续按过期时间移除旧码。

其他对接 API 的人员必须同步遵守轮询建议。项目原有后台统计、收件箱和管理接口仍会消耗 D1 额度，本次不承诺整个项目永远不超额。

## 检查范围

只进行源码静态检查、语法检查和打包检查，不执行回归测试；未代替用户执行远端数据库迁移。上线后可在 Cloudflare D1 的 Row Metrics 和 Worker 日志观察实际读取量。
