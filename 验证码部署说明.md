# 管理员无限地址收信与验证码查询

本目录来自 https://github.com/2014701726/cloud-mail ，独立于外层 Python 项目。
运行环境是 Cloudflare Workers + D1 + KV，附件沿用上游存储配置。无需 Outlook、IMAP、授权码。

## 新增功能

- 配置域名内尚未创建的地址，来信直接归入管理员主邮箱，不逐个创建账号。
- 实际 SMTP 收件地址保存在 `to_email`，因此转发、密送场景也能按实际投递地址查询。
- 已存在的邮箱及其权限保持上游行为；管理员原有“全部邮件”界面可查看全站邮件。
- “无限”表示不预建每一个地址，不代表 Cloudflare 的接收量、存储量或资源配额无限。
- 正文形如 `验证码为:800838,请在页面填写`，使用本地正则直接提取，不等待 AI。
  其他格式仍沿用上游可选 AI 提取。旧邮件不会自动重新提取验证码。
- 简洁手机页面，最新 5 条、最近 5 分钟、收件时间（北京时间）、一键复制、自适应轮询（正常约 3～5 秒，连续空结果可降至 15 秒）。
  页面请求失败时也会自行移除过期验证码。
- 无 recipient 不查询数据库，返回空列表。不移动或删除源邮件。

## 配置

编辑 `mail-worker/wrangler.toml` 的 `[vars]`（不要重复创建同名配置项）：

```toml
domain = ["example.com", "example.net"]
admin = "admin@example.com"
catch_all_admin = true
public_codes = true
code_all_recipient = "" # 设置自定义非空值后，该 recipient 可查全部收件人
code_allowed_senders = [] # 空列表允许所有发件人；例如 ["notice@example.com"]
```

Cloudflare 控制台手动添加 `domain`、`code_allowed_senders` 时，用 JSON 数组字符串。
`code_all_recipient` 和普通邮箱前缀均按去除首尾空白、转小写后计算 MD5。
公开页面和接口无需登录，知道对应前缀的访问者即可读取验证码，这是所要求的公开查询行为。

## 部署步骤

1. 按上游 README 的部署说明建立 D1、KV、附件存储等绑定，填写域名、管理员和 JWT 配置。
2. 使用上游初始化流程初始化数据库并注册/初始化配置的管理员。管理员主邮箱必须存在，其他地址不用创建。
3. 新版数据库初始化会自动建立查询索引。已有实例可执行
   `pnpm exec wrangler d1 execute <数据库名称> --remote --file=sql/code-query-index.sql`
   （在 `mail-worker` 目录执行）。此文件只添加索引。
4. 在 `mail-worker` 运行 `pnpm install --frozen-lockfile` 和 `pnpm run deploy`；上游构建流程会安装、打包 Vue 前端。
5. 为每个收件域名启用 Cloudflare Email Routing，完成其要求的 DNS 配置。
   开启 Catch-all，动作选择发送到本项目 Worker。若有单独地址路由，它可能优先于 Catch-all；
   需要接收的地址也必须路由到本 Worker。
6. 保持后台“接收邮件”开启。黑名单规则仍生效；开启 `catch_all_admin` 后，未建地址不会因“无收件人”开关而被拒收。

Cloudflare 官方说明：https://developers.cloudflare.com/email-service/configuration/email-routing-addresses/

## 查询链接（MD5 版，原明文链接停止支持）

收件人 `xlcvt@bingtangzhanghao.cn` 的前缀为 `xlcvt`，MD5 为 `4b19a9ed08986b4ab177375cbbafacc5`。

```text
https://mail.jx3saas.cn/codes?recipient=4b19a9ed08986b4ab177375cbbafacc5&domain=bingtangzhanghao.cn
https://mail.jx3saas.cn/api/codes?recipient=4b19a9ed08986b4ab177375cbbafacc5&domain=bingtangzhanghao.cn
```

先将前缀去除首尾空白、转小写，再按 UTF-8 求 MD5，输出 32 位十六进制。
不带 `domain` 时查所有配置域名。`code_all_recipient` 仍配置原始值，调用者传它按同一规则计算的 MD5；留空禁用。
当前索引优化版需先执行 `mail-worker/sql/code-hash-upgrade.sql` 再部署，不需要重新初始化数据库。详见 [D1读取优化升级说明](D1读取优化升级说明.md)。

完整请求、响应、错误和调用代码见 [API对接文档](API对接文档.md)。
